<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.server.mapper.LeaveMapper">

    <select id="selectByStudentNum" resultType="org.example.server.pojo.Leave">
        select * from Leave where "student_num" = #{student_num}
    </select>

    <select id="selectByStudentName" resultType="org.example.server.pojo.Leave">
        select * from Leave where "student_name" = #{student_name}
    </select>

    <select id="selectAll" resultType="org.example.server.pojo.Leave">
        select * from Leave
    </select>


    <insert id="insertLeave">
        insert into Leave (student_num, student_name,leave_type,leave_reason,destination,time,status,age,institute,major,instructor_name,instructor_tele,leave_detailed_reason,start_time,end_time,student_tele)
        values (#{student_num}, #{student_name}, #{leave_type}, #{leave_reason},#{destination},#{time},#{status},#{age},#{institute},#{major},#{instructor_name},#{instructor_tele},#{leave_detailed_reason},#{start_time},#{end_time},#{student_tele})
    </insert>

    <update id="updateStatus">
        update Leave set "status" = #{status} where "student_num" = #{student_num} and "start_time" = #{start_time} and "end_time" = #{end_time} and "leave_type" = #{leave_type} and "leave_reason" = #{leave_reason} and "destination" = #{destination} and "institute" = #{institute} and "major" = #{major} and "instructor_name" = #{instructor_name} and "instructor_tele" = #{instructor_tele} and "leave_detailed_reason" = #{leave_detailed_reason} and "student_tele" = #{student_tele};
    </update>



</mapper>