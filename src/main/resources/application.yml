spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:src/main/resources/db/demo.db
    hikari:
        maximum-pool-size: 1
server:
  port: 8080


mybatis-plus:
  configuration:
  map-underscore-to-camel-case: true
